# 2026-09-26 — สปส.1-10 report on the roster template

## What shipped (roster export untouched)
- **สปส.1-10 รายเดือน** menu item on the payroll page → `sso-1-10-YYYY-MM.xlsx`:
  same 6-column template + ลำดับที่, title block (form ref, company,
  account/branch, month), รวม footer (headcount, wages, both contributions),
  single sheet named by branch (default 000000).
- **Correct values**: month-aggregated actual wages via `monthClose.owed`
  (one ceiling); everyone with pay incl. leavers + daily; contractors,
  over-60, zero-wage skipped with counts; invalid IDs/names block with names.
- **Employer identifiers**: `sso_account_no` / `sso_branch_no` on
  `client_profiles` + company-settings section (digits-only, branch defaults
  000000) with 42703 pre-migration fallback; report hints when account unset.
- Engine: `buildSso110Rows` + `buildSso110Workbook`; 4 tests (inclusion,
  skips, blocking errors, template parity + totals + branch sheet).

## Verification
- vitest tests/payroll: 118/118. tsc clean; design check passed; build OK.
- No commit yet; awaiting review/commit decision.

## ⚠️ PENDING — manual migration apply
`supabase/migrations/20260926000001_sso_employer_identifiers.sql`
(`schema.sql` snapshot updated). Until applied, SSO identifier saves fall
back gracefully (main fields still save) and the report prints blanks.
